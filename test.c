#include <stdio.h>
#include <stdlib.h>

int main() {
/*
int i[3] = {1,2,3};
	printf("i = %d, %d, %d\n", i[0], i[1], i[2]);
	int* a = malloc(10);
	*a = 5;
	*a = 8;
	free(a);
	*/
	int y = sizeof(int);
	printf("%d\n", y);	
	return 0;
	}
